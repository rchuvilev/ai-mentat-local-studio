node_modules/
/dist/
/electron-main.bundle.js
.DS_Store

# copied from shared/ into the repo root at bundle time.
# Anchored to root so the source copies in shared/ stay tracked.
/update-ui.js

# signing material — never commit
certs/

# downloaded engines, models and generated media — large, machine-local
/engines/
/models/
/outputs/
/.local-studio/
app-updates-repo/

# Transpiled TypeScript tests (generated by scripts/build-ts.js)
.ts-build/
